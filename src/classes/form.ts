export class Form {
  nome: string;
  email: string;
  mensagem: string;

  constructor(nome: string, email: string, mensagem: string) {
    this.nome = nome;
    this.email = email;
    this.mensagem = mensagem;
  }
}

